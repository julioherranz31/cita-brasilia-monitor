name: Telegram Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Send test message to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ 8246994744:AAEqO4B0nm0e8ryd1D1Uxq43B7StpbxfBKQ }}
          TELEGRAM_CHAT_ID: ${{ 6651786553 }}
        run: |
          echo "Token starts with: ${TELEGRAM_BOT_TOKEN:0:6}..."
          echo "Chat id: $TELEGRAM_CHAT_ID"
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d "chat_id=$TELEGRAM_CHAT_ID" \
            -d "text=✅ TESTE OK: GitHub Actions conseguiu enviar mensagem." \
            | tee response.json
          echo ""
          echo "=== Telegram API response ==="
          cat response.json
